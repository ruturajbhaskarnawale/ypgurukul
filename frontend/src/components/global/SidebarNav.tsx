"use client";

// SidebarNav has been removed as part of the home page simplification.
// The vertical section navigator (Roman numerals I–VI) is replaced by the
// standard top Navbar. This file is kept as a null export to avoid
// breaking any imports that may still reference it.

export const SidebarNav = () => null;
